This represents a website idea for the AI Native Agency (Project: Agent Lab). It is being build at Manus bit by bit on a free account. 
As workflows get automated or agents get trained they will either get patched through here or be given their own repos but will fork them together.
